<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.appbar.AppBarLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbarRootLayout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:elevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/crownToolbar"
            style="@style/CrownToolbarDefault"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:contentInsetStart="40dp"
            app:navigationIcon="@null"
            tools:background="@color/__background_darker"
            tools:title="Title"
            tools:titleTextColor="@color/__text_high">
            <!--            tools:navigationIcon="@drawable/ic_arrow_back_black_24dp"-->

            <Button
                android:id="@+id/crownToolbarButtonRight"
                style="@style/Button_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_gravity="end"
                android:layout_marginEnd="16dp"
                android:background="?android:selectableItemBackground"
                android:textColor="@color/onboard_gradient_start"
                android:textSize="14sp"
                tools:text="cancel" />
        </androidx.appcompat.widget.Toolbar>

        <View
            android:id="@+id/crownToolbarShadow"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@drawable/toolbar_dropshadow" />
        <!--        android:background="@color/__shadow_toolbar_signup" />-->
    </LinearLayout>

</com.google.android.material.appbar.AppBarLayout>

